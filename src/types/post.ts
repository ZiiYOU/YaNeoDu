export type Post = {
  user_id: string,
  nickname: string,
  license_name: string,
  is_confirm: boolean,
  category: string,
  title: string,
  content: string,
  comments: null,
  views: number,
}
